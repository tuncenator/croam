# croam

Cross-host claude-code session manager. Tracks ownership, enables attach/peek/claim/fork across machines via SSH + syncthing, wraps claude in tmux for session persistence.

## Setup

```bash
uv sync
uv run croam --help
```
